import { makeStyles } from '@material-ui/core/styles';


export const useStyles = makeStyles( () => ({
    main: {
        width: "300px",
        height: "300px",
        margin: "20px auto",
        padding: "0 auto"
    }
}));